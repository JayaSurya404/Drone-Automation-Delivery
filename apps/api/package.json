{
  "name": "@skynav/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^11.3.0",
    "@fastify/jwt": "^10.2.2",
    "@skynav/config": "workspace:*",
    "@skynav/contracts": "workspace:*",
    "argon2": "^0.45.1",
    "fastify": "^5.2.0",
    "kysely": "^0.29.5",
    "pg": "^8.23.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "tsx": "^4.19.0"
  }
}
